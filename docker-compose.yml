services:
  flight_data_mcp:
    build: .
    ports:
      - "8002:8002"
    env_file: .env
    environment:
      - MCP_TRANSPORT=sse
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8002
    restart: unless-stopped
